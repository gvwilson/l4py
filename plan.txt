# L4PY Lesson Plan

### Lesson 6: Logic and Proofs

- Propositions as types (Curry-Howard basics)
- example and by blocks
- rfl, simp tactics
- calc for equational reasoning

### Lesson 7: Dependent Types

- Vector α n — lists that know their length
- Fin n — bounded numbers
- Why types depending on values eliminates bugs

---

### To Do

Types revealed that `Option` and `List` are inductive types.
IO could reveal that `IO` is also an inductive type, or that `do` desugars
to `bind` calls.  Even a one-sentence note — "Behind the scenes, `do` and
`←` desugar to function calls just like the `match` expressions in the
Types lesson" — would create continuity.

